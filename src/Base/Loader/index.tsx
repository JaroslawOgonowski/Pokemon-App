import { LoadingText, StyledLoader, StyledPokeball } from "./styled";

export const Loader = () => (
  <StyledLoader>
    <LoadingText>Loading...</LoadingText>
    <StyledPokeball />
  </StyledLoader>
);
